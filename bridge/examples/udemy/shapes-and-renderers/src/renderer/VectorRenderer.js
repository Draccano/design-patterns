class VectorRenderer {
    renderCircle(radius) {
        console.log('Drawing a circle of radius ' + radius);
    }
    renderSquare(points) {
        console.log('Drawing a square with side ' + points);
    }
}

module.exports = VectorRenderer;